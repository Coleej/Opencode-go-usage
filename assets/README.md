# Assets

Place a screenshot of the widget here as `screenshot.png` before submitting the registry PR.

Capture it with the popout open and real data visible, ideally on the default dank purple theme.
The registry uses this URL:

    https://raw.githubusercontent.com/Coleej/Opencode-go-usage/main/assets/screenshot.png
